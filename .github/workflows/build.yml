name: OrangeFox Recovery Build (tapas)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Sunucuyu Tamamen Temizle (Yer Aç)
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/boost /usr/lib/jvm
          # Bu komut yaklaşık 30GB+ yer açarak derleme için alan sağlar

      - name: Bağımlılıkları Kur
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential zip curl libc6-dev libncurses5-dev libssl-dev imagemagick python3 git-core

      - name: OrangeFox Kaynaklarını Hafif Çek
        run: |
          mkdir ~/fox
          cd ~/fox
          # Sadece son commit'i çekerek zamandan ve diskten tasarruf ediyoruz
          git clone --depth 1 https://gitlab.com/OrangeFox/misc/scripts.git -b master
          cd scripts
          # Android derleme ortamını hazırlar
          sudo bash setup/android_build_env.sh
          
- name: Tapas Cihaz Ağacını Ekle
        run: |
          cd ~/fox
          # Klasörü temizleyip yeniden oluşturuyoruz
          rm -rf device/xiaomi/tapas
          mkdir -p device/xiaomi/tapas
          # URL'yi daha geniş bir izinle ve farklı bir formatla deniyoruz
          git clone --depth 1 http://github.com/Paimon-Lab/android_device_xiaomi_topaz -b fox_12.1 device/xiaomi/tapas

      - name: Derlemeyi Başlat
        run: |
          cd ~/fox
          # Bu aşamada OrangeFox senin cihazın için derlenmeye başlar
          # Not: Eğer buraya kadar hatasız gelirse, bir sonraki adımda sana asıl 'make' komutunu vereceğim
          echo "Cihaz ağacı hazır, derleme ortamı kuruldu."
